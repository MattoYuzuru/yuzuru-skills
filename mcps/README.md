# MCP Registry

No MCP server is shipped in the first engineering-suite release. The requested capabilities are
local workflows, calculations, validation, and artifact assembly; an MCP service would add
operational and credential complexity without corresponding value.

Future servers must follow `docs/authoring/mcp.md` and declare owning plugins before any manifest
references them.
