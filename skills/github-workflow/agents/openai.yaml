interface:
  display_name: "GitHub Workflow"
  short_description: "GitHub repos, Projects, PR review, merge, and Actions"
  default_prompt: "Use $github-workflow to manage this GitHub repository, project board, pull request, review, merge, or Actions workflow safely."
