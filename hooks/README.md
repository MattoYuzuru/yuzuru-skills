# Hook Policy

Shared policy lives in `docs/authoring/hooks.md`. Runtime hook definitions stay inside the plugin
that packages and tests them; this directory does not provide an implicit global hook.
