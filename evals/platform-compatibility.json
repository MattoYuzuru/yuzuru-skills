{
  "version": 1,
  "cases": [
    {
      "id": "codex-enable",
      "agent": "codex",
      "expect": "Return interaction_required and the /plugins action; do not rewrite config."
    },
    {
      "id": "claude-disable",
      "agent": "claude",
      "expect": "Delegate to claude plugin disable and preserve native state across repository updates."
    },
    {
      "id": "plugin-data",
      "agent": "both",
      "expect": "Mutable plugin state uses host data variables or XDG data, never the installed plugin root."
    },
    {
      "id": "marketplace-paths",
      "agent": "both",
      "expect": "Codex loads .agents/plugins/marketplace.json and Claude loads .claude-plugin/marketplace.json."
    },
    {
      "id": "isolated-native-test",
      "agent": "both",
      "expect": "Native marketplace and plugin tests use temporary HOME/config/data and no real credentials."
    },
    {
      "id": "dsh-profile-bundle",
      "agent": "dsh",
      "expect": "Install a config-only bundle through the native profile manager, mount one isolated skill provider, and keep canonical SKILL.md content progressively loaded without copies."
    },
    {
      "id": "messenger-mcp-hosts",
      "agent": "both",
      "expect": "Codex and Claude load a real packaged .mcp.json runtime; host approval does not replace semantic authorization for writes."
    },
    {
      "id": "messenger-dsh-limit",
      "agent": "dsh",
      "expect": "Expose portable messenger skills but state that the config-only bundle does not launch the packaged MCP runtime."
    },
    {
      "id": "telegram-native-target",
      "agent": "both",
      "expect": "Load the checksummed macOS arm64 TDLib bundle and fail closed with NATIVE_RUNTIME_UNAVAILABLE on unshipped targets."
    }
  ]
}
